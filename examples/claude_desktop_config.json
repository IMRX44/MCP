{
  "comment": "Add this block to claude_desktop_config.json (Claude Desktop -> Settings -> Developer -> Edit Config). Adjust the path to where you cloned this repo. The MCP server and Cheat Engine use the current user's TEMP directory for file IPC; if you override CE_MCP_TEMP, set it before launching both processes.",
  "mcpServers": {
    "cheat-engine": {
      "command": "python",
      "args": ["-m", "ce_mcp"],
      "env": {
        "PYTHONPATH": "C:\\path\\to\\MCP\\src",
        "CE_MCP_TIMEOUT": "300",
        "CE_MCP_LOG_LEVEL": "info"
      }
    }
  }
}
